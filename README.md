# Salsa Course GIFs

Per-move animated GIFs for the 12-week salsa course static site.
One GIF per move, weeks 1-11. ~480px longest side, 3s, palette-optimized.

Served publicly via raw.githubusercontent.com and referenced from classes.js `gifUrl`.
Note: most timestamps are auto-derived and flagged needs-review — verify the moment matches the move.
